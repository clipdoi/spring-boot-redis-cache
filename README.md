Example spring boot redis cache simple
Use h2 db
save data to db
api rest:
+ get data user
+ update data in db, auto update data in cache
+ delete data in db, as well as data in cache which is removed